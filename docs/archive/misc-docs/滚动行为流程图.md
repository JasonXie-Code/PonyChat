# 滚动行为流程图

## 对话模式滚动流程

```mermaid
flowchart TD
    A[用户发送消息] --> B[调用 scrollToLastUserGroupTop]
    B --> C[滚动用户消息到屏幕顶部]
    C --> D[设置对话模式锚点<br/>_anchorMode = 'chatMode']
    D --> E[AI开始输出]
    E --> F{AI输出是否<br/>碰到底部?}
    F -->|否| G[保持不动<br/>shouldAutoScroll = false]
    F -->|是| H[清除锚点<br/>开始跟随滚动]
    H --> I[流式输出期间<br/>持续滚动到底部]
    I --> J[AI输出完成]
    
    K[用户触摸屏幕] --> L[触发 onUserTouchInterrupt]
    L --> M[清除锚点模式]
    M --> N[设置 _isTouching = true]
    N --> O[停止所有自动滚动]
    O --> P[用户可自由滚动]
    
    style C fill:#e1f5ff
    style G fill:#fff4e1
    style H fill:#e8f5e9
    style O fill:#ffebee
```

## 游戏模式滚动流程

```mermaid
flowchart TD
    A[用户点击选项] --> B[调用 scrollToLastGroup]
    B --> C[滚动用户消息到<br/>好感度胶囊下方]
    C --> D[设置动态填充<br/>保持用户消息位置]
    D --> E[AI开始输出]
    E --> F[完全禁止自动滚动<br/>shouldAutoScroll = false<br/>shouldStreamFollow = false]
    F --> G[用户自行阅读]
    G --> H[AI输出完成]
    
    I[用户触摸屏幕] --> J[触发 onUserTouchInterrupt]
    J --> K[记录日志<br/>游戏模式本来就不自动滚动]
    K --> L[用户可自由滚动]
    
    style C fill:#e1f5ff
    style F fill:#fff4e1
    style K fill:#f3e5f5
```

## 触摸打断机制

```mermaid
flowchart TD
    A[用户触摸屏幕] --> B[触发 touchstart 事件]
    B --> C[设置 _isTouching = true]
    C --> D[调用 onUserTouchInterrupt]
    D --> E{当前模式?}
    E -->|对话模式| F[清除锚点模式<br/>clearAnchorMode]
    E -->|游戏模式| G[记录日志<br/>游戏模式本来就不自动滚动]
    F --> H[停止自动滚动]
    G --> H
    H --> I[shouldAutoScroll 返回 false]
    I --> J[用户可自由滚动]
    
    K[用户松开手指] --> L[触发 touchend 事件]
    L --> M[延迟300ms后<br/>设置 _isTouching = false]
    M --> N{是否在底部附近?}
    N -->|是| O[清除填充<br/>恢复自动滚动能力]
    N -->|否| P[保持用户滚动位置]
    
    style C fill:#ffebee
    style F fill:#e8f5e9
    style H fill:#fff4e1
    style O fill:#e1f5ff
```

## 完整交互流程（对话模式）

```mermaid
sequenceDiagram
    participant U as 用户
    participant S as sendMessage
    participant R as renderChat
    participant SM as ScrollManager
    participant AI as AI输出流
    
    U->>S: 发送消息
    S->>R: renderChat(forceScrollToLastUser: true)
    R->>SM: scrollToLastUserGroupTop(true)
    SM->>SM: 滚动用户消息到顶部
    SM->>SM: setChatAnchor() 设置锚点
    Note over SM: _anchorMode = 'chatMode'
    
    AI->>SM: 开始流式输出
    loop 每次内容更新
        AI->>SM: 检查 shouldAutoScroll()
        SM->>SM: 检查 _isLastMessageReachingBottom()
        alt AI输出未碰到底部
            SM-->>AI: shouldAutoScroll = false
            Note over SM: 保持不动
        else AI输出碰到底部
            SM->>SM: clearAnchorMode()
            SM->>SM: scrollToBottom()
            Note over SM: 开始跟随滚动
        end
    end
    
    U->>SM: 触摸屏幕
    SM->>SM: onUserTouchInterrupt()
    SM->>SM: clearAnchorMode()
    SM->>SM: _isTouching = true
    Note over SM: 停止所有自动滚动
```

## 完整交互流程（游戏模式）

```mermaid
sequenceDiagram
    participant U as 用户
    participant H as handleGalgameOption
    participant R as renderChat
    participant SM as ScrollManager
    participant AI as AI输出流
    
    U->>H: 点击选项
    H->>R: renderChat(forceScrollToLastGroup: true)
    R->>SM: scrollToLastGroup(true)
    SM->>SM: 滚动用户消息到好感度胶囊下方
    SM->>SM: 设置动态填充
    Note over SM: 保持用户消息位置
    
    AI->>SM: 开始流式输出
    loop 每次内容更新
        AI->>SM: 检查 shouldAutoScroll()
        SM->>SM: 检查 isGalgameMode
        SM-->>AI: shouldAutoScroll = false
        Note over SM: 完全禁止自动滚动
    end
    
    U->>SM: 触摸屏幕
    SM->>SM: onUserTouchInterrupt()
    Note over SM: 记录日志（游戏模式本来就不自动滚动）
    Note over SM: 用户可自由滚动
```

## 关键实现验证

### ✅ 对话模式：发送消息 → 用户消息到屏幕顶部
- **实现位置**: `js/chat/chat-api.js:424` → `js/chat/message-ui.js:1341` → `js/core/scroll-manager.js:1258`
- **验证**: `scrollToLastUserGroupTop()` 方法正确实现，计算偏移量并滚动到顶部

### ✅ AI 输出在屏幕内时保持不动
- **实现位置**: `js/core/scroll-manager.js:889-895`
- **验证**: `shouldAutoScroll()` 检查锚点模式，如果 `_isLastMessageReachingBottom()` 返回 false，则返回 false

### ✅ AI 输出碰到底部时开始跟随
- **实现位置**: `js/core/scroll-manager.js:890-895`
- **验证**: 当 `_isLastMessageReachingBottom()` 返回 true 时，清除锚点并允许滚动

### ✅ 游戏模式：点击选项 → 用户消息到好感度胶囊下方
- **实现位置**: `js/chat/chat-api.js:423` → `js/chat/message-ui.js:1338` → `js/core/scroll-manager.js:1058`
- **验证**: `scrollToLastGroup()` 方法正确计算偏移量（包含好感度胶囊高度）

### ✅ 触摸打断：任何时候触摸屏幕都会停止自动滚动
- **实现位置**: `js/core/scroll-manager.js:821-851`
- **验证**: `touchstart` 事件触发 `onUserTouchInterrupt()`，设置 `_isTouching = true`，`shouldAutoScroll()` 检查此标志并返回 false

## 代码关键点

1. **锚点机制** (`scroll-manager.js:58-73`): 对话模式使用锚点控制滚动时机
2. **触摸检测** (`scroll-manager.js:820-851`): 监听 touchstart/touchend/wheel 事件
3. **模式判断** (`scroll-manager.js:860-899`): `shouldAutoScroll()` 根据模式返回不同结果
4. **动态填充** (`scroll-manager.js:1146-1234`): 游戏模式使用动态填充保持用户消息位置
