# Server-USA 后端迁移到本机验收记录

完成日期：2026-09-09。工作目录：`P:\PonyChat`。

## 完成结果

聊天、CosyVoice 网关、SearXNG 搜索及应用日志已经迁至本机并运行。手机 App `5.6.36 / 376` 使用 `http://39.101.74.217:80`，经 Server-CN 回环 18500 的 SSH 隧道连接本机 5000。官网保留在 USA，下载入口无缓存转发本机最新正式签名 APK。

- 本机、CN IP、USA 兼容 API 的健康响应均返回 `local-20260909-cn-ip-5.6.36`。
- USA 原 `ponychat-backend`、`ponychat-cosyvoice`、`ponychat-searxng` 均为 `LoadState=not-found / inactive`。
- USA 的 5000、18010 现在由 SSH 隧道监听；18786 不再监听。既有 Qwen 18012 隧道继续使用。
- USA 旧后端、测试运行目录与应用数据清理共移除 **696,278 个文件，22.37 GB 文件内容**，其中包括本次临时数据库导出及 **181 个旧 APK（2.91 GB）**。
- 清理后 USA 可用空间约 **40.70 GB**。这是当时的文件系统快照，会随其他业务变化。

## 数据完整性

正式业务数据及日志共有 **656,147 个文件、16,451,753,649 字节**，加当前主数据库 **1,004,453,888 字节**，合计 **17.46 GB（16.26 GiB）**。该合计不重复计入压缩传输包、依赖环境和原有本机 Qwen 模型。

| 内容 | 文件数 | 原始文件大小（十进制） |
| --- | ---: | ---: |
| 聊天历史日志 | 648149 | 15.9562 GB |
| 后端运行日志 | 4 | 0.0000 GB |
| 旧格式聊天日志 | 42 | 0.0002 GB |
| 测试日志 | 10 | 0.0010 GB |
| 状态及数据库辅助文件 | 5 | 0.0025 GB |
| 附件与网盘数据 | 42 | 0.2411 GB |
| 恢复状态 | 117 | 0.0153 GB |
| 知识及角色数据 | 7574 | 0.1307 GB |
| CosyVoice 生成音频 | 140 | 0.0611 GB |
| CosyVoice 上传音频 | 64 | 0.0437 GB |
| 当前 SQLite 主数据库 | 1 | 1.0045 GB |

首次全量复制后只需补齐两个发生变化的辅助/运行日志文件。最终扫描与补齐文件均核验 SHA-256，未发现缺失、内容差异或扫描错误。

数据库先停止服务器写入并 checkpoint，再通过 19 个变化块补齐本机文件。复制后的完整 SHA-256 与停机源库一致：

```text
3389322e7bbe2cd4bff968cb03ff4287905eb4bd2cdccad0d407846b1bfee963
```

SQLite `integrity_check=ok`。所有数据库字节一致证明表内容一致；表行数在本机完整检查，包含 **65 张表、121 个用户、439 个角色、627 个普通会话、70,888 条普通消息**。核验后仅转换三张日志索引表的路径列：641,951 条日志索引、648,117 条扫描进度、12 条索引错误记录；行数保持不变。日志 API 抽查三条历史详情均返回 200，索引中遗留 `/opt/ponychat/` 路径为 0。

附加保留了经逐文件哈希验证的服务器原始代码（1,331 个文件 / 110.31 MB）和测试运行记录（19,745 个文件 / 51.64 MB），存为本机私密压缩包，不覆盖已有不同版本代码。三个服务的历史 systemd 日志导出为 77.60 MB gzip，解压内容 981.51 MB，已验证 gzip 完整性。

本机原有同名且不同的配置、数据库和日志保存在 `var/migration-20260909/preexisting-local`。已移除本次多余传输包和临时数据库，共释放约 6.44 GB 本机空间。

## 功能验收

| 项目 | 结果与边界 |
| --- | --- |
| 本地正式服务 | 计划任务运行，聊天、搜索、CosyVoice、CN/USA 两条隧道均由本地守护进程管理 |
| 真实聊天与搜索 | 隔离测试库中的真实 Agent 请求通过；两个搜索工具调用、记忆保存、计费核对及无效鉴权检查通过 |
| Qwen3TTS | 8 个连续语音场景通过，6 个生成 MP3 完整解码通过 |
| CosyVoice | 真实云端合成完成，159,884 字节 WAV 完整解码通过 |
| 单元测试 | 17 项后端测试、85 项 Android 单元测试全部通过 |
| Android 构建 | Release 与 Debug 均成功；公开发布的是正式产品签名 Release |
| 模拟器 IP 路由 | Debug 5.6.36 在 emulator-5556 发送真实登录请求；CN Nginx 记录 OkHttp 的 `/api/auth/login`，无效测试密码得到预期 401；没有成功登录生产账号或作废其 token |
| APK 完整下载 | CN IP 和官网在清理前后均完整下载成功，字节数与 SHA-256 一致 |
| Clash 防绕行 | `39.101.74.217/32` 为首条 DIRECT 规则；Mihomo 校验与热加载成功，实际 CN SSH 22 和 HTTP 80 的 TUN 连接均命中 `IPCIDR / DIRECT`，完整 APK 下载哈希一致 |
| 保留网页 | 官网根域、www、CosyVoice 页面、歌曲页、乐谱页均返回 200；CosyVoice HTML 与原服务器文件哈希相同 |

没有把模拟器验收表述为实体手机安装验收；连接的实体手机未获得 ADB 授权，未改动其 App。真实聊天模型测试使用隔离数据，生产入口以健康、版本、完整下载和历史日志读取验收，未向真实用户会话插入测试聊天。

20:34 本地守护进程记录一次连续三次健康检查超时，并自动重启聊天服务。后续公网、日志与下载验收恢复通过；现有日志不足以确定该次超时原因，不将自动恢复表述为从未发生超时。20:49 为应用 CN 直连规则，主动关闭了本套服务的旧 CN 代理隧道连接，守护进程自动重连并验证 DIRECT。

版本化配置输出到 `C:\Users\Jason Xie\Downloads\VPN-USA Clash_v1.yaml`；原文件 SHA-256 保持不变，代理节点与已有远程控制直连规则保持不变。当前 Clash 本地配置及运行配置已应用首条 `IP-CIDR,39.101.74.217/32,DIRECT,no-resolve`，规则模式、TUN 均启用。配置验证与实际连接证据位于 `var/migration-20260909/clash-cn-direct`；带节点凭据的 YAML 不提交 Git。

## APK 发布

- 版本：`5.6.36`，`versionCode 376`。
- 本机分发文件：`P:\PonyChat\var\releases\PonyChat-v5.6.36-376-release.apk`。
- 大小：`12,009,820` 字节。
- SHA-256：`cb9020259b45bc840b1dca6407953771f26e92da38471d1ac9de2ae0b069b36e`。
- 正式签名证书 SHA-256：`a91fd0637ac948ed0a6ef86e3e5fdb7868bbb9d011498ef7c0145a41c3de4eb1`。
- 官网：`https://www.ponychat.org/download/apk`；App 更新：`http://39.101.74.217:80/download/apk`。
- USA 两处 PonyChat 发布目录中的直接 APK 文件均为 0；新下载代理关闭 `proxy_buffering`、`proxy_cache` 并设置 `proxy_max_temp_file_size 0`。
- 最新指针是本机 `var/releases/latest.json`。发布脚本验证完整包后原子切换，后端每次读取，无需重启聊天。

## 保留与运维边界

服务器历史备份及 `.bak`、`.before-*` 文件没有搬到本机、没有删除；共享系统 journal 由系统保留策略管理，本次仅导出三个服务的记录，不清除其他业务的共享日志。原部署归档保留为历史备份。网页静态资源、歌曲和乐谱均留在 USA；422 个登记的受保护文件在清理前后大小及修改时间一致，歌曲/乐谱进程 PID 678 / 676 均未改变。

本机新增的正常定时备份沿用原策略，写入 `var/backups`；这与搬迁服务器历史备份不同。Qwen 模型原本就在 `C:\PonyChatVoice\TTS`，本次没有迁移模型目录。

`PonyChat Local Backend Stack` 在 Windows 用户登录时自动启动。电脑开机、联网且用户登录后才可提供聊天与下载；休眠/关机/断网会使依赖本机的入口暂不可用。独立 Qwen 自启任务继续启用。未通过重启整台电脑验证登录触发，已核对任务配置并实际启动、验证子进程重启。

日常操作见 [SERVER.md](../../SERVER.md)、[后端部署](../../Backend/deploy/README.md)、[Android 发布](../../Android-App/README.md)。本次所有私密证据位于 `var/migration-20260909`，Git 只提交代码、文档与不含私钥的 APK 发布元数据。
