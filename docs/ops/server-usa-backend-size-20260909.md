# Server-USA PonyChat 后端文件占用盘点（2026-09-09）

盘点时间：北京时间 **2026-09-09 19:01:26–19:01:53**。目标：Server-USA（154.17.23.237）。只读统计，未停止服务、删除文件或迁移。

## 结论

| 范围 | 当前文件磁盘占用 | 普通文件内容字节合计（未压缩） |
|---|---:|---:|
| 聊天主后端 + 语音网关 + 搜索，保留运行数据 | 2.41 GB | 2.34 GB |
| 上述三项及其全部已识别日志、备份、测试文件 | 39.72 GB | 38.04 GB |
| 再加歌曲库、自动乐谱：五个后端的运行文件 | 6.31 GB | 6.22 GB |
| 五个后端及全部已识别相关文件 | **43.62 GB（40.62 GiB）** | **41.92 GB** |

GB = 1,000,000,000 字节；GiB = 1,073,741,824 字节。内容大小和磁盘占用不同；没有打包，因此不把这些数当作压缩包或网络传输大小。

“不含网页”按排除前端页面、构建产物和 APK 分发文件处理。歌曲、乐谱 API 单独列出，便于选择迁移范围。运行文件口径保留全部现有知识资料、网盘（含回收站）、恢复状态及语音网关音频；不是通过删除数据得到的最低安装体积。

## 所有相关文件的分项（不重复加总）

| 项目 | 服务器路径 | 磁盘占用 GB |
|---|---|---:|
| 聊天主后端（程序、依赖、生产数据、目录内历史文件） | `/opt/ponychat；排除 PonyChat-Website` | 22.574363 |
| CosyVoice 网关（含依赖、音频上传与输出） | `/opt/ponychat-cosyvoice；排除 static` | 0.162513 |
| SearXNG 搜索（含依赖） | `/opt/ponychat-searxng` | 0.177803 |
| 歌曲库 API、依赖与媒体 | `/opt/mlp-music + /data/mlp-music` | 3.746882 |
| 自动乐谱 API、依赖与数据 | `/opt/mlp-music-auto + /data/mlp-music-auto` | 0.148963 |
| 外置部署备份 | `/var/backups/ponychat*；排除 APK 备份` | 16.637030 |
| 后端临时测试目录与文件 | `/tmp、/var/tmp 中已识别的后端文件` | 0.169312 |
| 专属服务配置与孤立 SQLite sidecar | `/etc/ponychat、systemd 配置、/var/lib/ponychat-model-pause、/root/ponychat.db-*` | 0.000115 |

合计 **43,616,980,992 字节 = 43.616981 GB**。

## 主后端目录内的构成

| 项目 | 磁盘占用 |
|---|---:|
| 生产数据库目录（不含备份；含 WAL/SHM） | 1.006621 GB |
| Python 虚拟环境 | 0.651907 GB |
| 后端程序（含现有编译缓存） | 0.010801 GB |
| 角色知识资料 | 0.146461 GB |
| 网盘数据（含回收站） | 0.241230 GB |
| 恢复快照等运行状态 | 0.016024 GB |
| LLM 请求/响应调试日志 | 17.464652 GB |
| 数据库备份 | 2.927354 GB |
| 开发脚本、测试及报告 | 0.109015 GB |

调试日志共 **648,149 个文件**，占用 **17.46 GB**，普通文件内容约 **15.96 GB**。这些是请求/响应诊断日志；运行文件口径仍保留生产数据库，不将调试日志等同于用户聊天记录。

数据库备份约 **2.93 GB**；外置部署备份约 **16.64 GB**。盘点期间其他工作产生了新的部署备份，早期检查与最终快照存在差异，应采用上面的最终快照。

## 服务与迁移关联

| systemd 服务 | 监听端口 | 快照状态 |
|---|---:|---|
| ponychat-backend | 5000 | active / running |
| ponychat-cosyvoice | 18010（本地回环） | active / running |
| ponychat-searxng | 18786（本地回环） | active / running |
| mlp-music | 8822（本地回环） | active / running |
| mlp-music-auto | 8823（本地回环） | active / running |

现场配置确认：默认语音提供方为 `voice_lab`，引擎 `qwen3tts`，访问 `http://127.0.0.1:18012`；该端口由 SSH 隧道监听。部署文件说明其连接本机 Windows 的 Qwen3TTS。**本机模型权重不在本次 Server-USA 文件大小内**；服务器上的 CosyVoice 网关单独保留在统计中。

所有虚拟环境的 Python 解释器符号链接指向 `/usr/bin/python3.12`。Linux 虚拟环境不能直接作为 Windows 原生环境使用；本机需要建立对应运行环境，最终本机占用会有差异。这里统计的是应用文件，不是完整 Linux 系统镜像大小。

迁移时，数据库必须使用一致性备份或协调停写；本次文件大小统计不是可直接恢复的数据库备份。还需要带上实际 `.env`、专属服务配置与数据路径设置；本报告仅记录配置文件大小及安全的路径/引擎字段，未复制密钥内容。

## 排除项和测量边界

- 排除主站、管理网页、MBTI 网页、音乐网页、CosyVoice 网页及 APK 发布文件。`/opt/ponychat/PonyChat-Website` 的约 2.70 GB 主要为 APK，整项排除；`/var/www` 前端站点不计入。
- SearXNG 安装包自带资源按依赖保留，以反映完整已安装搜索服务体积；没有将 PonyChat 前端网站算入。
- 排除共享系统 Python、操作系统、Nginx/Redis 安装体积、系统 journal、公共 Nginx 日志、包管理器缓存、其他业务及本机模型。共享日志无法据此精确分摊到 PonyChat 的物理占用。
- Redis 当次 `INFO keyspace` 为空，客户端列表只出现盘点连接；代码检索仅发现可选依赖检查，没有证据表明它保存本次要迁移的 PonyChat 数据。MBTI 为静态站，未发现独立 MBTI 服务；LLM 的旧 18020 上游没有监听。
- 对已识别目录逐项读取元数据，按设备号和 inode 去重；包含目录块开销，不跟随符号链接，不跨文件系统。15 个指向目录外的链接均为系统 Python 解释器，没有发现遗漏的外部业务数据链接。
- `/dev/shm` 中瞬时 Agent 测试数据属于内存文件系统，单独记录并排除磁盘总计。临时测试进程和文件保留原状。
- 五个服务持续运行，其他部署同时进行，统计不是原子快照；日志、WAL 和备份可以继续变化。最终元数据扫描未报告文件读取错误。

逐字节分项、根路径、服务状态、符号链接及计算组成见 [原始统计 JSON](server-usa-backend-size-20260909.json)。
