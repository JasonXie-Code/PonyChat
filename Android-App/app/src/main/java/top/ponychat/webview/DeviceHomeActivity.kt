package top.ponychat.webview

/**
 * Device-only HOME entry for the same PonyChat APK.
 * It is disabled in the stock APK and enabled by the self-developed device system image via RRO.
 */
class DeviceHomeActivity : MainActivity()
