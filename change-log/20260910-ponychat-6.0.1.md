# PonyChat 6.0.1 / 379

- 关系页始终保留双头像，加载时显示动画占位条，失败提示居中并通过下拉重试。
- 关系页生成采用独立优先队列，避免读取页面使运行中的任务失效，减少多余请求与轮询间隔。
- Agent 卡展示当前及最近工具，统一切换菜单样式，工具阶段累计预算为 40 秒。
- 包含网址阅读与小马 Wiki 查询约束，以及已测试的提示词调整。
- 发布前后端针对性回归 270 项通过；真实模型与模拟器耗时记录见关系加载优化报告。
