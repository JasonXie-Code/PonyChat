# 2026-09-07 个人偏好与更多面板

- 普通聊天：个人偏好、定时任务、小游戏、陪玩四个入口。
- 游戏 / 锁分：启用更多按钮，仅展示个人偏好。
- 编辑区域沿用 @ 选择面板的页内覆盖形式，使用 PonyChat 圆角输入区、主题色、PonyButton 和 PonyIconButton；支持键盘避让、加载重试、保存失败保留编辑内容。
- 云端设置按 `账号 / personal_preferences / 角色 ID / 模式` 保存。只提交当前模式的字段；清空并保存取消偏好。
- 用户自行设置的偏好优先于角色默认表达风格及历史推测偏好，保留游戏规则和输出协议约束。普通聊天与游戏 / 锁分提示词均接入。
- 普通对话重置、游戏重置和锁分重置均不清除个人偏好。

## 验证

- Android `:app:assembleDebug` 通过；安装到 `emulator-5554`，保留原有应用数据。
- 三种模式的入口、偏好保存及重新读取、模式隔离、清空保存实测通过；页内编辑面板与键盘展开布局已截图检查。
- 后端 87 项测试通过，包括账号 / 角色 / 模式隔离和三种真实重置路径的偏好保留测试。
- Server-USA 仅部署 `request_context.py` 与 `personal_preferences.py`；文件 SHA-256 与本地一致，服务 active，公网 `/api/health` 返回 HTTP 200 / running。
- 在线提示词组装确认锁分测试偏好不会进入普通聊天或游戏模式；测试偏好已全部清空。未重置用户真实对话。
- 回滚备份：`/var/backups/ponychat-personal-preferences/20260907-011202`。
- 本次安装为模拟器 Debug APK，未发布公众下载版本。

## 5.6.21 / 361：退出自动保存

- 移除个人偏好底部取消和保存按钮，扩大编辑区域。
- 关闭叉号、左上角返回与系统返回统一走自动保存；未修改内容直接退出，保存失败保留编辑内容并提示重试。
- 清空后退出会取消当前模式偏好；加载未完成时退出不会写入空值。
- 模拟器实测关闭、返回后重新打开读取正确；测试内容已清空。
- Release / Debug 构建通过；正式签名 APK 发布为 `PonyChat-v5.6.21-361-release.apk`。
- `www.ponychat.org` 与 `ponychat.org` 版本接口均返回 `5.6.21 / 361`，下载入口哈希一致，服务健康检查通过。
- 正式 APK SHA-256：`c2e53cb045236effa9c96a98f8c7e71f6a784a68b155c2f83fa1752368265ed3`。
- 版本配置备份：`/var/backups/ponychat-apk/5.6.21-361-1788716016495108994`；模拟器已安装同版本 Debug 构建并保留原应用数据。

## 5.6.22 / 362：进入面板不显示进度条

- 个人偏好加载时不再显示进度条，加载完成前继续保护编辑状态；退出自动保存保持原有行为。
- Release 构建和正式签名验证通过，在线 APK 与版本接口已更新。
- 两个正式域名的版本与下载哈希核验通过，后端健康检查正常。
- 正式 APK SHA-256：`4e8bcdf86d325f0eea4bf34c544926a0ab8f743de4e9435358c9013263d3c61f`。
- 版本配置备份：`/var/backups/ponychat-apk/5.6.22-362-1788716425081038970`。
