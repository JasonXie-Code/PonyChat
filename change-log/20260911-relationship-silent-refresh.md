# 6.0.8 / 386：关系页面静默更新

- 关系页面已有内容时，消息触发的数据更新和下拉刷新保持内容显示，返回新数据后原位更新文字，不再切换整页加载面板。
- 自动更新网络失败、超时或后台整理失败时保留同角色、同会话的已有内容；失败后仍可重新请求。首次加载和关系设置变更清空内容后的加载、失败提示保留。
- 验证：Debug 构建、85 项单元测试通过；模拟器两项界面测试通过，覆盖连续刷新保留内容、原位更新文字、首次加载及失败后下拉重试。
- 6.0.8 / 386 正式签名 APK 已发布；本机、CN 和官网版本接口一致，三个下载入口的完整文件 SHA-256 均与签名产物一致。发布回执见 `docs/testing/relationship-silent-refresh-20260911/public-release.json`。后端无需重启。
