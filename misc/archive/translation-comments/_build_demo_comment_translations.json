{
  "# Global measure→page table, populated by _autodetect_page2_first_measure().": "# 全局小节→页码表，由 _autodetect_page2_first_measure() 填充。",
  "# Key: first measure number (1-based) of each page; Value: page number.": "# 键：每页首个小节号（1 起）；值：页码。",
  "# For 2-page songs this is {1: 1, PAGE2_FIRST_MEASURE: 2}.": "# 两页歌曲为 {1: 1, PAGE2_FIRST_MEASURE: 2}。",
  "# For N-page songs it is extended with more entries.": "# N 页歌曲会追加更多条目。",
  "        # Extend upward enough to cover chord symbols (≈1.5× staff height above)": "        # 向上扩展以覆盖和弦符号（约五线谱上方 1.5 倍谱表高度）",
  "        # and downward enough to cover one line of lyrics (≈1.5× staff height below).": "        # 向下扩展以覆盖一行歌词（约下方 1.5 倍谱表高度）。",
  "                    # For margin calculation use a SINGLE staff height, not the full": "                    # 计算边距时用单个谱表高度，而非整个",
  "                    # multi-staff system span.  For single-staff systems these are the": "                    # 多谱表系统跨度。单谱表系统两者相同；",
  "                    # same; for multi-staff systems (PB + H1 etc.) this prevents the": "                    # 多谱表（PB + H1 等）可避免边距",
  "                    # margin from blowing up to cover neighbouring systems.": "                    # 膨胀到覆盖相邻系统。",
  "            # Keep PAGE2_FIRST_MEASURE in sync for backward-compat code paths": "            # 保持 PAGE2_FIRST_MEASURE 与向后兼容代码路径同步",
  "            # Printed measure numbers live at the left of a staff system.  This": "            # 印刷小节号位于谱表系统左侧。此处",
  "            # deliberately excludes tempo markings such as \"134\" near the title.": "            # 刻意排除标题附近的速度标记如 \"134\"。",
  "            # PAGE2_FIRST_MEASURE has already been auto-detected by": "            # PAGE2_FIRST_MEASURE 已由",
  "            # _autodetect_page2_first_measure() before this call.": "            # _autodetect_page2_first_measure() 在本调用前自动检测。",
  "            # Build a per-page measure offset table.  Prefer the global page": "            # 构建每页小节偏移表。优先使用由 PDF 印刷小节号",
  "            # starts derived from printed PDF measure numbers; OMR local measure": "            # 推导的全局页起始；OMR 局部小节",
  "            # IDs can undercount crowded pages and shift later systems forward.": "            # ID 可能低估拥挤页面，使后续系统前移。",
  "        # Sort by physical score position: measure → beat → notes-before-rests → part (top to bottom).": "        # 按谱面物理位置排序：小节 → 拍 → 休止前音符 → 声部（上到下）。",
  "        # This matches the left-to-right, top-to-bottom order that Audiveris assigns notehead IDs,": "        # 与 Audiveris 分配符头 ID 的从左到右、从上到下顺序一致，",
  "        # which is NOT the same as linearTime order when multiple parts are interleaved.": "        # 多声部交错时与 linearTime 顺序不同。",
  "        # Printed measure numbers sit near the top-left of a system.  Using the": "        # 印刷小节号靠近系统左上角。在此使用",
  "        # lyric/staff-band row matcher here is unsafe for two-staff systems,": "        # 歌词/谱表带行匹配器对双谱表系统不安全，",
  "        # because the next measure number can fall inside the previous system's": "        # 因下一个小节号可能落在上一系统",
  "        # lower staff rectangle.": "        # 下方谱表矩形内。",
  "        # If a previous heuristic put the indicator between systems, snap it": "        # 若先前启发式将指示器放在系统之间，将其",
  "        # back to the nearest detected system rectangle.": "        # 吸附回最近的已检测系统矩形。",
  "                # Use a larger gap for lyric-to-lyric transitions so that a": "                # 歌词间过渡用更大间隔，使",
  "                # forced-forward lyric word is visible for at least 100 ms.": "                # 被强制前移的歌词至少可见 100 ms。",
  "    # Pre-compute max time for each (page, row_bucket) so that when we cross": "    # 预计算每个 (page, row_bucket) 的最大时间，以便跨",
  "    # into a new row we use the true high-water mark of the previous row rather": "    # 入新行时使用上一行真实高水位，而非",
  "    # than just the immediately-preceding event in score order.  A secondary": "    # 仅谱序中紧邻事件。次要",
  "    # voice / staff note may sit at an earlier beat (and thus appear first in": "    # 声部/谱表音符可能位于更早拍（谱序更靠前）",
  "    # score order) but carry a later corrected timestamp from pdf-asr-time,": "    # 但携带 pdf-asr-time 修正后的更晚时间戳，",
  "    # meaning the simple \"previous event\" reference is not conservative enough.": "    # 故简单「上一事件」参考不够保守。",
  "        # Detect a visual boundary: either a page change or a row change": "        # 检测视觉边界：换页或换行",
  "        # (same page, but the visual y-position jumps to a new row).": "        # （同页但视觉 y 跳到新行）。",
  "        # Use the maximum time across ALL events on the previous row (not just": "        # 用上一行所有事件的最大时间（不限于",
  "        # the immediately-preceding score event) as the effective lower bound.": "        # 谱序紧邻事件）作为有效下界。",
  "        # If the required push is very large (>= 3 s) this is almost certainly": "        # 若所需前移很大 (>= 3 s)，几乎肯定是",
  "        # a repeat-sign scenario where the same visual row is visited at two": "        # 反复记号场景：同一视觉行在两种",
  "        # different audio times.  Forcing the event forward would create a": "        # 音频时间被访问。强制前移会造成",
  "        # massive in-row inversion for the earlier pass.  Skip it here and rely": "        # 较早段落严重行内倒置。此处跳过，依赖",
  "        # on enforce_visual_row_time_order to clean up within-row ordering.": "        # enforce_visual_row_time_order 整理行内顺序。",
  "        # Also update the row_max_time for the current row to reflect the push.": "        # 同时更新当前行 row_max_time 以反映前移。",
  "        # For page crossings cascade forward to nudge the leading events of the": "        # 跨页时级联前移以推动新页",
  "        # new page that share the same compressed timestamp. For row crossings": "        # 共享同一压缩时间戳的首批事件。跨行",
  "        # apply the same cascade but limit to events on the same new row.": "        # 同样级联但限于同一新行事件。",
  "                # For page crossings: scan ALL events on the first new-page row": "                # 跨页：扫描新页首行所有事件",
  "                # and push any whose time is still below the boundary threshold.": "                # 并将仍低于边界阈值的时间前移。",
  "                # Events already safely past the threshold are skipped (continue),": "                # 已安全超过阈值的事件跳过 (continue)，",
  "                # while events that need a very large push (>= 3 s) are also skipped.": "                # 需极大前移 (>= 3 s) 的也跳过。",
  "                    # Off the first row: use original close-cascade logic": "                    # 非首行：用原紧密级联逻辑",
  "                # Same large-push guard: skip if this would move the event by >= 3 s": "                # 同样的大前移防护：移动 >= 3 s 则跳过",
  "        # Rename to canonical name for future calls": "        # 重命名为规范名供后续调用",
  "    # Check if there are multiple .mxl files that need merging": "    # 检查是否有多个需合并的 .mxl 文件",
  "        # Delete stale merged musicxml so merge is re-triggered when mxl content changes": "        # 删除过期的合并 musicxml，mxl 变更时重新合并",
  "            # Re-merge if any mxl is newer than the merged file": "            # 任一 mxl 新于合并文件则重新合并",
  "    # Parse all movements": "    # 解析所有乐章",
  "        # Only one readable movement": "        # 仅一个可读乐章",
  "    # Detect namespace": "    # 检测命名空间",
  "    # Build base parts dict": "    # 构建基础 parts 字典",
  "    # Find the max measure number in a part": "    # 查找 part 中最大小节号",
  "    # Append each subsequent movement's parts onto base parts": "    # 将后续各乐章 parts 追加到 base parts",
  "        # Determine offset from base": "        # 确定相对 base 的偏移",
  "        # Find a common offset for parts not yet in base": "        # 为尚未在 base 中的 part 找公共偏移",
  "            # Deep-copy the measures from this movement part": "            # 深拷贝本乐章 part 的小节",
  "            # Renumber": "            # 重新编号",
  "                # Append measures to existing part": "                # 将小节追加到已有 part",
  "                # New part from this movement: create a skeleton and insert": "                # 本乐章新 part：创建骨架并插入",
  "    # Also merge part-list entries from subsequent movements (avoid duplicates)": "    # 合并后续乐章 part-list 条目（避免重复）",
  "    # Count merged measures": "    # 统计合并后小节数",
  "                # Exclude title-sized quoted song names and chord OCR noise.": "                # 排除标题级引号歌名与和弦 OCR 噪声。",
  "            # Use the persistent current_time_sig_beats as the authoritative measure length.": "            # 用持久 current_time_sig_beats 作为权威小节长度。",
  "            # max_local_beat is only used as a fallback when no time signature is available,": "            # max_local_beat 仅在无拍号时作回退，",
  "            # or to detect a genuine extension (e.g. 5/4 pickup measures).": "            # 或检测真实延长（如 5/4 弱起小节）。",
  "            # Keep missing/empty measures represented in the rough timeline.": "            # 粗略时间轴仍保留缺失/空小节。",
  "            # Avoid filling the melody stream with harmony notes when a lyric note exists at the same beat.": "            # 同拍已有歌词音时避免用和声音填充旋律流。",
  "    # Approximate from measure number and beat within measure. The MusicXML parser": "    # 由小节号与小节内拍近似。MusicXML 解析器",
  "    # has already normalized the melody notes onto the same rough beat grid.": "    # 已将旋律音归到同一粗略拍网格。",
  "        # Suppress DC / very low frequencies (< ~100 Hz) which are dominated by bass": "        # 抑制 DC / 极低频 (< ~100 Hz)，多为低音主导",
  "    # OMR sometimes attaches tempo/style markings as fake lyrics near the top": "    # OMR 有时将速度/风格标记当作假歌词附在顶部",
  "    # of the first system. Treat isolated all-symbol/very-short tokens there as": "    # 首系统。将该处孤立全符号/极短 token 视为",
  "    # non-vocal so ASR anchors do not pull the intro out of shape.": "    # 非人声，避免 ASR 锚点拉歪前奏。",
  "        # 8% of peak is conservative: counts breath/light noise as silence,": "        # 峰值 8% 较保守：将呼吸/轻噪计为静音，",
  "        # actual notes (even soft pad chords) are above this on every MLP cue.": "        # 实际音符（含柔和 pad 和弦）在各 MLP cue 均高于此。",
  "        # Open with a 3-frame structuring element so single-frame dips inside": "        # 用 3 帧结构元素开运算，避免持续音内",
  "        # a sustained note are not mis-labeled as silence.": "        # 单帧下陷被误判为静音。",
  "        # Mismatch must be MORE expensive than 2 gaps (= 1.70) so the aligner": "        # 不匹配代价须高于 2 个间隙 (= 1.70)，对齐器",
  "        # prefers skipping unmatched score lyrics or unmatched ASR words instead": "        # 才会跳过未匹配谱面歌词或未匹配 ASR 词，",
  "        # of forcing a wrong pairing.  Without this, songs whose score has gaps": "        # 而非强行错配。否则谱面歌词有空缺时",
  "        # of missing lyrics (e.g. multi-staff scores where Audiveris drops": "        # （如 Audiveris 在多谱表上丢失",
  "        # lyrics in some measures) get all later score words pulled toward": "        # 部分小节歌词）后续谱词会被拉向",
  "        # earlier ASR words, ruining the timing.": "        # 更早 ASR 词，破坏时序。",
  "    # Use the first groups of the melodic thread (cap at 3 for search cost).": "    # 使用旋律线首几组（上限 3 以控制搜索成本）。",
  "    # Restrict to audio hits that lie *meaningfully earlier* than the vocal-slope": "    # 限于明显早于 vocal-slope",
  "    # back-extrapolation.  Searching the whole intro maximised recall on stray": "    # 反向外推的音频命中。搜整段前奏会提高",
  "    # repeating motifs instead of the true opening gesture.": "    # 杂散重复动机召回而非真实开场手势。",
  "    # --- Step 1: estimate vocal slope (audio sec per score sec).": "    # --- 步骤 1：估计 vocal slope（音频秒/谱面秒）。",
  "    # We compute TWO slopes and use the smaller one for backward extrapolation:": "    # 计算两条 slope，反向外推取较小者：",
  "    #   * local_slope: median pairwise slope across the first ~8 vocal anchors.": "    #   * local_slope：前 ~8 个 vocal 锚点的成对 slope 中位数。",
  "    #     Picks up the chorus-level cadence but is biased high when vocals are": "    #     捕捉副歌级律动，但人声 rubato 时偏高。",
  "    #     sung with rubato (each syllable drifts later than notation).": "    #     （每音节比记谱更晚。）",
  "    #   * global_slope: how much audio time has passed by the first vocal note": "    #   * global_slope：首个 vocal 音时已过多少音频时间",
  "    #     vs. score linear time at that point.  This is closer to the song's": "    #     相对该点谱面 linear time。更接近歌曲",
  "    #     long-term tempo ratio and is the safer choice for projecting backward": "    #     长期 tempo 比，反向外推更安全：",
  "    #     through the instrumental intro: a too-steep slope drags the score's": "    #     过陡 slope 会把谱面",
  "    #     first instrumental note ahead of where the recording actually plays": "    #     首个器乐音拉到录音实际播放之前",
  "    #     it (the bug that motivated this whole intro-anchor pass).": "    #     （本 intro 锚点流程要修的 bug）。",
  "    # --- Step 2: where does the recording's musical content actually start?": "    # --- 步骤 2：录音音乐内容实际从哪开始？",
  "    # Use the sustained onset when it is meaningfully later than the bare": "    # 当 sustained onset 明显晚于裸",
  "    # onset detector — that gap is the song's pre-roll content and should": "    # onset 检测器时采用前者 — 该间隙为 pre-roll，",
  "    # be excluded from the score intro.": "    # 应从谱面前奏排除。",
  "    # --- Step 3: pitch-matched intermediate intro anchors.": "    # --- 步骤 3：音高匹配的中间 intro 锚点。",
  "    # These let the cursor track tempo changes inside the intro itself.": "    # 使光标能跟踪前奏内的 tempo 变化。",
  "        # Opening-target matching may prove that the *first* notated melody": "        # 开场目标匹配可能证明首个记谱旋律",
  "        # note starts very early, but using that early onset as the seed for": "        # 音很早开始，但用该早 onset 作",
  "        # every later pitch match compresses the whole intro and makes the": "        # 后续 pitch 匹配种子会压缩整段前奏，使",
  "        # cursor lead by roughly a measure.  Keep the conservative vocal-slope": "        # 光标约超前一小节。保留保守 vocal-slope",
  "        # seed for the tempo-tracking chain; the early opening note remains a": "        # 种子作 tempo 链；早开场音仍是",
  "        # real anchor above, so the initial rest is still skipped when justified.": "        # 真实锚点，合理时仍跳过初始休止。",
  "        # Rotate the audio histogram backwards by k so audio_pc[(orig - k) % 12]": "        # 将音频直方图后旋 k，使 audio_pc[(orig - k) % 12]",
  "        # lines up with score_pc[orig].  A non-zero best k means the audio is": "        # 与 score_pc[orig] 对齐。best k 非零表示音频",
  "        # transposed by +k semitones relative to the score.": "        # 相对谱面移调 +k 半音。",
  "    # Only declare a transposition when the best shift is meaningfully better": "    # 仅当最佳移位明显优于不移位时才声明移调，",
  "    # than no-shift; otherwise default to 0 to avoid accidental damage on": "    # 否则默认 0，避免对齐良好录音",
  "    # well-aligned recordings.": "    # 被误伤。",
  "        # Skip the first instrumental group (already used as the anchor": "        # 跳过首个器乐组（已作锚点",
  "        # endpoint) and anything past the first vocal anchor.": "        # 终点）及首个 vocal 锚点之后内容。",
  "        # Apply the detected transposition so a recording in a different key": "        # 应用检测到的移调，使不同调录音",
  "        # does not silently fall back to \"any audio onset is a match\".": "        # 不会静默回退为「任意 onset 即匹配」。",
  "        # Adaptive search window: longer gaps tolerate larger drift.": "        # 自适应搜索窗：间隔越长容忍漂移越大。",
  "            # Require that the notes actually matching the score pitch classes": "            # 要求实际匹配谱面音级的音符",
  "            # are strong enough – rejects faint accompaniment harmonics.": "            # 足够强 — 拒绝微弱伴奏泛音。",
  "    # First vocal time = earliest non-intro anchor": "    # 首个 vocal 时间 = 最早非 intro 锚点",
  "    # Find last non-rest intro note (well before vocals)": "    # 找前奏末个非休止音（远早于 vocal）",
  "    # Find last strong audio note of that PC before vocal (ignoring very start of note)": "    # 找该 PC 在 vocal 前最后强音频音（忽略音头极始）",
  "    # Safety: only worth doing if push is > 0.8 s and room remains": "    # 安全：仅当 push > 0.8 s 且仍有空间时值得做",
  "    # Collect all rest events in the pre-vocal block": "    # 收集 pre-vocal 块内所有休止事件",
  "    # Mark the first rest as a fixed snap boundary so onset-snap redistribution": "    # 将首个休止标为固定 snap 边界，避免 onset-snap 重分配",
  "    # does not drag it back to the linearly-interpolated position.": "    # 将其拖回线性插值位置。",
  "        # Some official recordings compress notated rests or intros by many": "        # 部分官方录音将记谱休止/前奏压缩许多",
  "        # seconds. Keep a generous bound here; the sequence alignment and": "        # 秒。此处保留宽松界；序列对齐与",
  "        # monotone filter below are the real guards against wrong repeats.": "        # 下方单调滤波才是真正防错反复。",
  "    # Slope sanity filter: a healthy alignment between score and audio should": "    # Slope 合理性滤波：健康的谱音对齐",
  "    # have d_asr / d_linear close to 1.0 (within ~[0.4, 2.5]).  When score": "    # d_asr / d_linear 应接近 1.0（约 [0.4, 2.5]）。谱面",
  "    # lyrics get mis-matched to far-away ASR words (e.g. M13 \"per\" pulled to": "    # 歌词错配到 distant ASR 词（如 M13 \"per\" 被拉到",
  "    # an early \"you're\"), the local slope between consecutive anchors gets": "    # 早期 \"you're\"），相邻锚点局部 slope",
  "    # extreme.  Drop the offender.  We do this iteratively until all": "    # 极端。丢弃肇事锚点，迭代直至",
  "    # remaining slopes look reasonable.": "    # 剩余 slope 合理。",
  "            # Find the worst slope ratio (deviation from 1.0)": "            # 找最差 slope 比（偏离 1.0）",
  "                # Allow generous bounds: 0.30 to 3.0": "                # 宽松界：0.30 到 3.0",
  "            # Drop the anchor that lies on the bad edge.  Prefer dropping the": "            # 丢弃坏边上的锚点。优先丢",
  "            # anchor whose neighbours look more consistent with each other.": "            # 邻居彼此更一致的锚点。",
  "                # Drop right endpoint of bad edge": "                # 丢弃坏边右端点",
  "                # Drop left endpoint": "                # 丢弃左端点",
  "                # Default: drop later one (preserves earlier good context)": "                # 默认丢较晚者（保留较早好上下文）",
  "            # When basic-pitch proves the melodic chain starts earlier than the": "            # 当 basic-pitch 证明旋律链早于",
  "            # slope back-extrapolation, pinned leading rests must not consume any": "            # slope 反推， pinned 前导休止不得消耗",
  "            # audio time -- otherwise play-head ``t>0`` still highlights meas.~1.": "            # 音频时间 — 否则 play-head ``t>0`` 仍高亮 meas.~1。",
  "            # Three sub-cases:": "            # 三种子情况：",
  "            #   (a) There is a previous non-rest event before the first anchor": "            #   (a) 首个锚点前有先前非休止事件",
  "            #       (some unfilled instrumental gap).  Linearly compress": "            #       （未填器乐间隙）。在彼事件与首锚间",
  "            #       between that event and the first anchor.": "            #       线性压缩。",
  "            #   (b) No previous non-rest exists (e.g. an all-rest intro": "            #   (b) 无先前非休止（如全休止前奏",
  "            #       before the first vocal/instrumental anchor).  Linearly": "            #       在首个 vocal/器乐锚点前）。从 (lin=0, audio=0)",
  "            #       scale from (lin=0, audio=0) up to (first_x, first_y) so": "            #       线性缩放到 (first_x, first_y)，使",
  "            #       the cursor advances at the same rate the audio actually": "            #       光标与音频实际播放同速前进。",
  "            #       plays.  This is what makes the intro of fully-rested": "            #       这使全休止前奏（catchy-song、",
  "            #       leadings (catchy-song, smile-song) line up with the": "            #       smile-song）与录音对齐，",
  "            #       recording instead of riding the noisy DTW path.": "            #       而非沿噪声 DTW 路径。",
  "            #   (c) Previous non-rest is geometrically inconsistent with the": "            #   (c) 先前非休止与首锚几何不一致",
  "            #       first anchor (would require zero/negative span); fall": "            #       （需零/负跨度）；回退到 (b)。",
  "            #       back to (b).": "            #       （同上）",
  "            # Chord symbols and layout labels live in the same PDF text layer.": "            # 和弦符号与布局标签同在 PDF 文本层。",
  "            # Keep only words that sit in the lyric band of a staff system and": "            # 仅保留位于谱表系统歌词带、",
  "            # can later be sequence-matched to ASR.": "            # 且可后续与 ASR 序列匹配的词。",
  "        # Long lyric rows with repeated phrases still work, but small chunks": "        # 长歌词行重复短语仍可用，小块",
  "        # make the local DP less likely to bridge unrelated phrases.": "        # 使局部 DP 不易桥接无关短语。",
  "        # Coarse pass: whole lyric row.  This establishes a local timing shift": "        # 粗 pass：整行歌词。建立本行局部时移，",
  "        # for the current visual row, so repeated phrases elsewhere cannot": "        # 避免别处重复短语抢走",
  "        # capture individual words.": "        # 个别词。",
  "        # Fine passes: phrase chunks and short sliding word windows.  These use": "        # 细 pass：短语块与短滑动词窗。使用",
  "        # the row shift from the coarse pass but keep a much smaller ASR window.": "        # 粗 pass 行移但 ASR 窗更小。",
  "        # If ASR/PDF cue timing says two neighbouring lyric events are nearly": "        # 若 ASR/PDF cue 时序显示相邻歌词事件几乎",
  "        # continuous, a notated short rest between them is likely compressed in": "        # 连续，其间记谱短休止可能在发行音频中",
  "        # the released audio. Suppress it so it cannot interrupt the cursor or": "        # 被压缩。抑制以免打断光标或",
  "        # force later cue events forward.": "        # 迫使后续 cue 前移。",
  "    # Mark critical events": "    # 标记 critical 事件",
  "            # A rest that was pinned by the intro-rest-push pass must act as a": "            # intro-rest-push 固定的休止须作",
  "            # snap boundary, otherwise the redistribution loop overwrites it.": "            # snap 边界，否则重分配循环会覆盖。",
  "    # Compute snap targets": "    # 计算 snap 目标",
  "        # Find closest onset (binary search)": "        # 找最近 onset（二分）",
  "        # Adaptive snap window:": "        # 自适应 snap 窗：",
  "        #  - Lyric events: ASR anchor already gave us ~50-100ms accurate timing,": "        #  - 歌词事件：ASR 锚点已给 ~50-100ms 精度，",
  "        #    so we ONLY snap when an onset is very close (≤120ms).  This avoids": "        #    仅 onset 很近 (≤120ms) 时 snap，避免",
  "        #    yanking the vocal cursor onto an instrumental hit that happens to": "        #    将 vocal 光标拉到碰巧早几百 ms 的",
  "        #    be a few hundred ms earlier than the actual sung onset.": "        #    器乐 hit 而非真实演唱 onset。",
  "        #  - Pure instrumental critical events have no lyric ground truth, so": "        #  - 纯器乐 critical 事件无歌词真值，",
  "        #    we allow a wider window (±0.25s) to let onsets drag DTW estimates": "        #    允许更宽窗 (±0.25s) 让 onset 拉动 DTW 估计",
  "        #    onto real attacks.": "        #    到真实起音。",
  "            # Keep instrumental snaps tight on the back side: a wide backward": "            # 器乐 snap 后侧收紧：宽后向窗会 latch",
  "            # window latches onto the previous beat or a ghost pad hit and drags": "            # 到上一拍或 ghost pad，拖整小节更早，",
  "            # whole measures earlier, which reads as the cursor leading the mix": "            # 读作光标领先混音",
  "            # (notably mid-intro piano on Love Is In Bloom).": "            # （如 Love Is In Bloom 前奏中段钢琴）。",
  "    # Enforce strict monotonicity in snap list (drop a snap if it would": "    # snap 列表严格单调（若 snap 会",
  "    # violate the order set by previous snap or break the natural sequence).": "    # 破坏先前 snap 顺序或自然序列则丢弃）。",
  "    # Apply snap with redistribution between consecutive snap pairs": "    # 在相邻 snap 对之间应用 snap 并重分配",
  "        # Set boundaries": "        # 设边界",
  "        # Redistribute interior events by linearTime ratio": "        # 按 linearTime 比重分配内部事件",
  "    # Mark snapped events": "    # 标记已 snap 事件",
  "    # Phrase re-anchoring is useful, but too aggressive while the score/audio": "    # 短语重锚有用，但谱音特征仍粗时过激进。",
  "    # feature model is still coarse. Keep the rest-specific mapping stable here.": "    # 此处保持 rest 专用映射稳定。",
  "        # Search over the notated rest span with extra tolerance for dialogue": "        # 在记谱休止跨度上搜索，对话/rubato 额外容差，",
  "        # or rubato, but keep it local to avoid grabbing a later refrain.": "        # 但保持局部以免抓到后段副歌。",
  "            # Very weak prior toward the current global solution; lets strong": "            # 对当前全局解极弱先验；强短语证据可移动锚点，",
  "            # phrase evidence move the anchor, but avoids random far jumps.": "            # 但避免随机远跳。",
  "        # Only stretch when the audio between the old and new phrase starts has": "        # 仅当旧新短语起点间音频",
  "        # real content. This catches dialogue/non-pitched material and avoids": "        # 有实内容时拉伸。捕获对话/无音高材料，避免",
  "        # manufacturing silence where the recording genuinely skipped the rest.": "        # 在录音确已跳过休止处造静音。",
  "            # Rests are visual/structural events. Prefer quiet frames, but make": "            # 休止为视觉/结构事件。偏好静帧，但",
  "            # them cheap enough to collapse when the audio has shortened silence.": "            # 足够便宜以便音频缩短静音时坍缩。",
  "        # Broad time prior: enough to keep sections in order, not enough to": "        # 宽时间先验：保段落顺序，不足以",
  "        # force the score's rests when the audio demonstrably moved on.": "        # 在音频已前进时强压谱面休止。",
  "            # Notes should usually move forward; rests are allowed to sit on the": "            # 音符通常应前移；休止可停在",
  "            # previous musical time when the audio compresses a notated pause.": "            # 上一音乐时间当音频压缩记谱停顿。",
  "        # Upper bound: next distinct time (leave a gap)": "        # 上界：下一 distinct 时间（留隙）",
  "        # Distribute within [t0, t_end] using linearTime ratios": "        # 在 [t0, t_end] 内按 linearTime 比分配",
  "    # If a notated rest belongs before the next sung note but DTW placed it": "    # 若记谱休止应在下一演唱音前但 DTW 将其",
  "    # later, move the rest just before that note. Do not delay the sung note.": "    # 放更后，将休止移到该音前。不延迟演唱音。",
  "    # Use spectral flux onset detection (much more sensitive to note onsets than RMS energy)": "    # 用 spectral flux onset 检测（比 RMS 能量更敏感于起音）",
  "    # Build reference signal: smeared impulses at estimated note positions": "    # 构建参考信号：在估计音位涂抹脉冲",
  "    # Tighter band: allow at most ~5% total duration drift (≈5s for a 99s song)": "    # 更紧带：总时长漂移最多 ~5%（99s 歌约 5s）",
  "    # This prevents the DTW from collapsing large note groups to a single onset peak": "    # 防止 DTW 将大音符组坍缩为单 onset 峰",
  "    # Fallback: old frame-level chroma DTW. Kept only as a safety net when the": "    # 回退：旧帧级 chroma DTW。仅作 event aligner 失败时",
  "    # event aligner fails, not as the normal path.": "    # 安全网，非正常路径。",
  "    # Build per-measure beat length: prefer MusicXML time signatures, fall back to": "    # 构建每小节拍长：优先 MusicXML 拍号，回退到",
  "    # max(beatInMeasure + noteDuration) observed in sync events.": "    # sync 事件中 max(beatInMeasure + noteDuration)。",
  "    # Keep root-level files as the default sample for older API paths and debugging.": "    # 根级文件作旧 API 路径与调试的默认样本。"
}
