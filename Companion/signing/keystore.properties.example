# Optional Companion-local signing configuration.
# By default Companion reuses ../Android-App/signing/keystore.properties so both APKs
# have the same signature identity. Keep this file's real copy and the JKS private.

storeFile=signing/ponychat-release.jks
storePassword=change-me
keyAlias=ponychat
keyPassword=change-me
