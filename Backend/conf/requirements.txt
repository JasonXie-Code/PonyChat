# AI Role-Play Dependencies

# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.12

# Data Validation
pydantic>=2.0.0,<3.0.0

# HTTP Client
httpx>=0.27.0,<1.0.0
tenacity
json-repair

# System & Monitoring
psutil>=7.0.0
anyio>=4.0.0
cryptography>=44.0.0

# Image Processing
Pillow>=10.4.0
# SVG rasterization (for auto image transcoding)
cairosvg>=2.8.0
svglib>=1.6.0
reportlab>=4.4.0

# Environment Variables
python-dotenv>=1.0.0

# Optional normal-engine selector uses this pinned official agent runtime.
deepseek-harness-sdk==0.1.2rc1

# Database
aiosqlite>=0.19.0

# Performance (recommended; matches production Server-USA)
# orjson: faster JSON; redis: optional cache client; uvloop: Linux only (not supported on Windows)
orjson>=3.9.0
redis>=5.0.0
uvloop>=0.19.0; platform_system != "Windows"
# Windows does not ship the IANA database used by ZoneInfo("Asia/Shanghai").
tzdata>=2026.1; platform_system == "Windows"
